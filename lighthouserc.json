{
  "ci": {
    "collect": {
      "url": [
        "http://localhost:8080/",
        "http://localhost:8080/hello-vayupress",
        "http://localhost:8080/os/",
        "http://localhost:8080/os/posts",
        "http://localhost:8080/os/media",
        "http://localhost:8080/os/settings"
      ],
      "numberOfRuns": 3,
      "settings": {
        "preset": "desktop",
        "//": "The console needs a session; the workflow's throwaway API key is one. Public pages render the same with it.",
        "extraHeaders": "{\"X-API-Key\":\"lh-ci-key\"}"
      }
    },
    "assert": {
      "assertMatrix": [
        {
          "//": "Public site. Target is 95+ across the board, reported as warnings: public pages vary with seeded content and runner speed.",
          "matchingUrlPattern": "^http://localhost:8080/(hello-vayupress)?$",
          "assertions": {
            "categories:performance": ["warn", { "minScore": 0.9 }],
            "categories:accessibility": ["warn", { "minScore": 0.95 }],
            "categories:best-practices": ["warn", { "minScore": 0.95 }],
            "categories:seo": ["warn", { "minScore": 0.95 }]
          }
        },
        {
          "//": "Console (Still Air). Accessibility and best practices are deterministic for a page, and scored 100 on all four when this was set, so anything less fails: a threshold of 0.95 passed a page at 96 with a real contrast failure. Byte budgets sit just above the 2026-09-26 measurement (total 352 KB, CSS 54 KB, JS 44 KB, fonts 97 KB); raise one on purpose, with the reason, when a requirement needs it. Performance timing varies between runners, so it warns. SEO is not asserted: the console is noindex by design.",
          "matchingUrlPattern": "^http://localhost:8080/os/",
          "assertions": {
            "categories:accessibility": ["error", { "minScore": 1 }],
            "categories:best-practices": ["error", { "minScore": 1 }],
            "categories:performance": ["warn", { "minScore": 0.9 }],
            "resource-summary:total:size": ["error", { "maxNumericValue": 450000 }],
            "resource-summary:stylesheet:size": ["error", { "maxNumericValue": 64000 }],
            "resource-summary:script:size": ["error", { "maxNumericValue": 56000 }],
            "resource-summary:font:size": ["error", { "maxNumericValue": 115000 }]
          }
        }
      ]
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
